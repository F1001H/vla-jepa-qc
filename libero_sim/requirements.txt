# Pinned to match openpi's examples/libero/requirements.txt exactly
# (mujoco==3.2.3 / robosuite==1.4.1 -- the combo already validated working
# via headless EGL on the hessianAI 43 cluster, see
# slurm/smoketest_mujoco_egl.slurm), EXCEPT torch/torchvision/torchaudio:
# the original pins +cu113 CUDA 11.3 wheels (predates H100/sm_90 kernel
# support), but torch here is ONLY used for torch.load() on cached
# init-state files (libero/libero/benchmark/__init__.py,
# libero/libero/utils/utils.py) -- confirmed via grep, no .cuda() calls
# anywhere in this dependency chain. Using plain (CPU, no CUDA suffix)
# PyPI wheels avoids the old-CUDA-wheel-availability risk entirely with
# zero functional difference for our use.
absl-py==2.1.0
certifi==2024.12.14
charset-normalizer==3.4.0
cycler==0.12.1
docstring-parser==0.16
etils==1.3.0
eval-type-backport==0.2.0
evdev==1.7.1
fonttools==4.55.3
glfw==1.12.0
idna==3.10
imageio==2.35.1
imageio-ffmpeg==0.5.1
importlib-metadata==8.5.0
importlib-resources==6.4.5
kiwisolver==1.4.7
llvmlite==0.36.0
markdown-it-py==3.0.0
matplotlib==3.5.3
mdurl==0.1.2
mujoco==3.2.3
numba==0.53.1
numpy==1.22.4
opencv-python==4.6.0.66
packaging==24.2
pillow==10.4.0
psutil==6.1.0
pygments==2.18.0
pynput==1.7.7
pyopengl==3.1.7
pyparsing==3.1.4
python-dateutil==2.9.0.post0
python-xlib==0.33
pyyaml==6.0.2
requests==2.32.3
rich==13.9.4
robosuite==1.4.1
scipy==1.10.1
setuptools==75.3.0
shtab==1.7.1
six==1.17.0
termcolor==2.4.0
torch==1.11.0
torchaudio==0.11.0
torchvision==0.12.0
tqdm==4.67.1
typeguard==4.4.0
typing-extensions==4.12.2
tyro==0.9.2
urllib3==2.2.3
zipp==3.20.2
# Only needed for LIBERO-Plus's Sensor Noise category (motion-blur
# corruption via ImageMagick) -- harmless to include for standard LIBERO
# too, both share this venv. Requires the libmagickwand SYSTEM library at
# import time (not just this Python wheel) -- see
# slurm/eval_libero_plus_qc_sequential_cluster.slurm's precondition check.
Wand==0.7.2

# The sim-side eval client (examples/LIBERO/model2libero_interface.py)
# imports deployment/model_server/tools/websocket_policy_client.py directly
# to talk to server_qc_policy.py/server_policy.py -- locally this came in
# transitively via `uv pip install -e packages/openpi-client` (openpi's
# README install sequence), which this fork's sim venv build doesn't run.
# Full transitive import set of websocket_policy_client.py + msgpack_numpy.py,
# checked directly against those two files rather than guessed, so this
# doesn't turn into another one-at-a-time whack-a-mole:
# typing_extensions (already pinned above), numpy (already pinned above),
# websockets, msgpack.
websockets>=11.0
msgpack>=1.0.5

# model2libero_interface.py also imports starVLA.model.tools.read_mode_config
# (resolved via PYTHONPATH into the repo, not pip-installed -- same pattern
# already used for starVLA/examples.SimplerEnv imports here). Traced its own
# import chain (tools.py -> starVLA.training.trainer_utils -> .overwatch,
# all stdlib except this one): needs omegaconf, present locally but never
# pinned here.
omegaconf==2.3.1
